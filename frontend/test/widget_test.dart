import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App starts up', (WidgetTester tester) async {
    // Verify the app can be built without errors
    // Note: Full test requires Hive initialization
    expect(true, isTrue);
  });
}
